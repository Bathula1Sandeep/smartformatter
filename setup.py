from setuptools import setup, find_packages
setup(
    name='smartformater',
    version='0.1',
    packages=find_packages(),
    install_requires=['inflect'],
    author='sandeep',
    description='Utility fuction for smart formating of names, phones, numbers.',
)